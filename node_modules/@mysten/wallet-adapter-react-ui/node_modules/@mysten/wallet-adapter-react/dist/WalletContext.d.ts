import { FC, ReactNode } from "react";
import type { SuiAddress, SuiTransactionResponse, SignableTransaction } from "@mysten/sui.js";
import { WalletAdapter, WalletAdapterList } from "@mysten/wallet-adapter-base";
export interface WalletContextState {
    adapters: WalletAdapterList;
    wallets: WalletAdapter[];
    wallet: WalletAdapter | null;
    connecting: boolean;
    connected: boolean;
    select(walletName: string): void;
    disconnect(): Promise<void>;
    getAccounts: () => Promise<SuiAddress[]>;
    signAndExecuteTransaction(transaction: SignableTransaction): Promise<SuiTransactionResponse>;
}
export declare const WalletContext: import("react").Context<WalletContextState | null>;
export interface WalletProviderProps {
    children: ReactNode;
    adapters: WalletAdapterList;
}
export declare const WalletProvider: FC<WalletProviderProps>;
export declare function useWallet(): WalletContextState;
